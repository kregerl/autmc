export enum MenuId {
    Instances,
    Screenshots,
    Logs,
}

export enum OpenModalType {
    None,
    SideMenu,
    Settings,
}

export enum InstanceType {
    Vanilla, 
    Curseforge,
    Modrinth,
    Zip
}

export enum ModloaderType {
    None,
    Fabric,
    Forge
}